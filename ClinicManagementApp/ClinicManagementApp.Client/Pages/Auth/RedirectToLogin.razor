@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        // Use client-side navigation without forceLoad for faster redirect
        NavigationManager.NavigateTo("/login", forceLoad: false);
    }
}